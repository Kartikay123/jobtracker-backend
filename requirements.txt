# --- Web framework ---
fastapi==0.115.4
uvicorn[standard]==0.32.0
gunicorn==23.0.0          # prod process manager (uvicorn workers)

# --- Database (async SQLAlchemy 2.x) ---
sqlalchemy[asyncio]==2.0.36
asyncpg==0.30.0
alembic==1.13.3

# --- Redis (async) ---
redis[hiredis]==5.2.0

# --- Config & validation ---
pydantic[email]==2.9.2
pydantic-settings==2.6.1

# --- Auth ---
python-jose[cryptography]==3.3.0
passlib[bcrypt]==1.7.4
bcrypt==4.0.1            # pinned: passlib + bcrypt 4.1+ has a known warning
python-multipart==0.0.12

# --- HTTP client (for outbound AI calls later) ---
httpx==0.27.2
openai==1.54.0

# --- File parsing (resume uploads) ---
pypdf==5.1.0

# --- Background jobs ---
arq==0.26.3

# --- Dev / test ---
pytest==8.3.3
pytest-asyncio==0.26.0
ruff==0.7.2
